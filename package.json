{
  "dependencies": {
    "zustand": "^5.0.12",
    "next": "16.2.4",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "tailwindcss-cli": "^0.1.2"
  },
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build"
  }
}
